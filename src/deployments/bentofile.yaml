service: "model_service:svc"
labels:
  owner: renz
  stage: staging
include:
  - "*.py"
  - "*.json"
  - "artifacts/"
python:
  packages:
    - numpy
    - pandas
    - scikit-learn
    - xgboost==0.90
docker:
  env:
    - NAMESPACE=staging
